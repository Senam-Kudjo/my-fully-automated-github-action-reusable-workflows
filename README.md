# my-fully-automated-github-action-reusable-workflows
I replicated my Jenkins shared library implementations on github actions using reusable workflow (a similar concept)

it would be best if you had a self hosted runner that already had some tools and access installed on it already like helm, access to resources on your cloud provider and kubectl.
